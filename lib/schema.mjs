import { z } from 'zod';
export const actions=['explain','translate','terms','paper-summary','experiment-flow','question','sentence','results'];
export const requestSchema=z.object({action:z.enum(actions),demo:z.boolean(),selection:z.string().max(12000),question:z.string().max(2000),page:z.number().int().positive(),depth:z.enum(['simple','detail','example']),complexity:z.enum(['简洁版','标准版','详细版']),pages:z.array(z.object({page:z.number().int().positive(),text:z.string().max(50000)})).max(300)});
export const answerSchema=z.object({title:z.string().max(300),answer:z.string().max(20000),terms:z.array(z.object({term:z.string(),translation:z.string(),simple:z.string(),detail:z.string(),technical:z.string(),prerequisites:z.array(z.string())})).default([]),steps:z.array(z.string()).default([]),mermaid:z.string().max(15000).default('')});
